# UE 5.7.3 PCG CircleComponent Case

## 问题背景
PCG 不能直接读取 `UCircleComponent`。当前目标不是“发明一种新曲线格式”，而是让圆形/圆弧几何进入现有 PCG 流程，并兼容 `Create Surface From Spline` 这类现有下游节点。

## 方案比较

### 1. 内置改源码
- 优点：节点直接进核心，调用链最短。
- 缺点：侵入 PCG 核心，维护面大，后续 UE 升级冲突风险高。

### 2. 插件互操作
- 优点：不改 PCG 核心，风险可控，容易回收和迁移，也更适合做对比验证。
- 缺点：语义上仍是“Circle -> Spline”的桥接，不是新几何数据类型。

### 3. 新数据类型 `CircleData`
- 优点：能保留圆心、半径、法线、角度、闭环等解析语义，适合后续扩展到 arc / ellipse / ring。
- 缺点：需要新增数据模型、转换节点和更多下游兼容工作，投入明显更大。

## 为什么先选插件
先用插件把需求验证清楚，再决定是否把抽象下沉到核心，是更稳妥的引擎开发路径。这样可以：
- 先验证工作流和交互是否成立。
- 直接比较核心改动和插件接入的维护成本。
- 让面试表达更像“架构决策”，而不是“我写了个节点”。

## 为什么输出 `SplineData`
当前 PCG 生态里已有大量节点消费 `PolyLine/SplineData`，`Create Surface From Spline` 也能直接接入。短期内把圆形转成 spline 是最有效的兼容路径。

这一步的核心价值是“保留几何来源并打通现有链路”，而不是强行给圆单独造一个数据类型。

## 自动化测试
已补两类自动化测试：
- `Plugins.PCG.CircleData.Correctness`
- `Plugins.PCG.CircleData.PerfLog`

测试覆盖：
- core / plugin 输出一致性
- helper 转换一致性
- closed loop 行为
- 圆组件执行性能

并且给执行链加了硬上限，避免异常情况下出现长时间空转。

## 实测性能
当前 headless automation 的真实结果：

- 启动编辑器并进入 automation 约 `14.68 s`
- 测试本体执行约 `0.1 s` 量级
- `PerfLog` 输出：

```text
core=9.793 ms total (612.050 us/iter) | plugin=10.021 ms total (626.306 us/iter) | helper=0.074 ms total (4.612 us/iter) | iter=16
```

这说明：
- 真正耗时主要在 editor / automation 启动，而不是测试逻辑本身。
- core 和 plugin 两条路径性能接近，差距很小。
- helper 只作为基线参考，成本最低。

## 兼容性与风险
- `SplineData` 会丢失圆的解析语义，精度受采样段数影响。
- 节点命名和模块加载需要注意，避免 editor 中出现重复入口或测试发现失败。
- 如果后续明确需要“真圆/真弧”的解析运算，应该上 `CircleData`，而不是把 spline 方案无限扩展。

## 后续演进
推荐路线是：
1. 先保留插件方案作为低风险互操作层。
2. 如有必要，再引入 `CircleData`，作为高语义几何数据。
3. 同时保留 `Circle -> Spline` 显式转换节点，确保现有 PCG 生态继续可用。

## 面试表达重点
这件事最值得讲的不是“我加了一个节点”，而是：
- 我比较了核心改造、插件互操作、数据模型三种方案。
- 我先用插件验证需求，降低了引擎核心侵入风险。
- 我用自动化测试和性能数据把方案从“能用”推进到“可证明”。
- 我知道什么时候应该停留在 `SplineData`，什么时候应该升级到 `CircleData`。

## CircleComponent 的价值
`CircleComponent` 的价值不在于它本身“更快”，而在于它把圆形这个场景里天然存在的几何源，直接接进了 PCG。

- 对内容生产来说，少了一次手工转 spline 或手工摆点的中间步骤。
- 对引擎集成来说，证明了 PCG 可以消费新的几何输入源，而不是只会处理传统 spline/point data。
- 对后续演进来说，`CircleComponent` 是一个可验证的入口，它能继续扩展到 arc、ellipse、ring 这类解析几何。

## 你可以怎么讲
### 一句话版本
我把场景里的 `CircleComponent` 接进了 UE5 PCG 管线，让圆形几何可以直接驱动现有生成流程，并且通过自动化测试和性能数据证明这条链路是可维护的。

### 30 秒版本
这个项目的重点不是“做了一个圆形节点”，而是我把一个 PCG 原本不认识的几何源，接入了现有的生成链路。  
我先比较了改核心、做插件互操作、引入 `CircleData` 三种方案，最后优先选了插件方式，原因是风险小、可回收、能快速验证工作流。  
短期内输出 `SplineData`，是为了直接兼容 `Create Surface From Spline` 这类现有节点；如果后续确实需要保留圆的解析语义，再升级到 `CircleData`。  
我还补了自动化测试和性能日志，最后能用数据说明这条链路是可用、可回归、可演进的。
