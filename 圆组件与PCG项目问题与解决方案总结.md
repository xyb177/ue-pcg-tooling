# 圆组件与 PCG 项目问题与解决方案总结

## 1. 项目背景

本项目围绕自研 `CircleComponent` 展开，目标是把圆组件接入 UE 的 PCG 流程，并同时支持源码版与插件版两条实现路径。

最终产物主要包括：

- 圆组件本体 `CircleComponent`
- `PCG Get Circle Data` 接入
- 编辑器 Details 面板定制
- 独立插件版 `CircleComponentPCG`
- 自动化测试与构建验证

---

## 2. 版本划分

项目中实际存在两条线：

### 2.1 源码版

- 直接修改 UE 源码树中的实现
- 适合验证底层行为、编辑器显示、PCG 接入链路
- 依赖引擎源码工程，不适合直接搬到其他 UE 安装中

### 2.2 插件版

- 将圆组件、Details 定制、PCG 互操作整理为独立插件
- 适合跨项目复用
- 可直接放到其他 UE 工程的 `Plugins` 目录下启用

### 2.3 版本管理原则

- 源码版和插件版要分开维护
- 不在同一工程里同时混用同名实现
- 每次修改前先确认当前目标是“源码验证”还是“插件交付”

---

## 3. 遇到的主要问题与解决方案

### 3.1 `Get Circle Data` 的来源混淆

**问题：**

- `Get Circle Data` 同时存在源码版和插件版实现
- 容易混淆当前生效的是哪一版

**解决方案：**

- 源码版保留在引擎源码树中，用于验证和回归
- 插件版单独抽离成 `CircleComponentPCG`
- 文档中明确标注两个版本的边界和用途

**结果：**

- 能清楚区分“源码验证”和“插件交付”
- 避免了同名实现互相干扰

---

### 3.2 `Full Angle` 的语义问题

**问题：**

- `FullAngle` 最初使用弧度语义，对美术和 TA 不够直观
- 曾尝试把整套 API 全部改成度语义，改动范围过大，导致维护复杂、行为容易混乱

**解决方案：**

- 保持内部计算链路原有语义不大改
- 只在编辑器 UI 层让 `FullAngle` 以度制输入和显示
- 在真正进入三角函数计算时再做度到弧度的转换

**结果：**

- 对外更符合美术习惯
- 内部逻辑避免大范围改写
- 降低了兼容性风险

---

### 3.3 缩放和圆大小的交互问题

**问题：**

- `Scale` 与 `Radius` 的关系曾被反复调整
- 一度出现过“锁死缩放”“无法拖 Gizmo 改变大小”“Reset 后半径失效”等问题
- 这些问题本质上来自于把缩放语义和几何语义混在一起处理

**解决方案：**

- 回到最小改动原则
- 先保证 Gizmo 拖动能正常改变大小
- `Reset` 必须能恢复默认半径和默认缩放
- 不再强行做过度的 `X/Y/Z` 统一同步或绝对缩放锁定

**结果：**

- 组件基础交互恢复可用
- 编辑器操作更稳定
- 避免了“改一处、坏一处”的连锁问题

---

### 3.4 `ResetToDefaultParams` 无响应

**问题：**

- 早期 Details 面板里 `ResetToDefaultParams` 按钮没有正确触发编辑器刷新

**解决方案：**

- 先确保 Reset 的逻辑只做必要的默认值恢复
- 避免在按钮里额外叠加复杂事务链和过多 UI 逻辑

**结果：**

- Reset 行为回到稳定状态
- 避免了按钮点击后状态和视图不同步

---

### 3.5 Details 面板信息过多、可编辑项不清晰

**问题：**

- `CircleCurves` 这类内部缓存字段对 TA 没有直接编辑意义
- Details 面板中暴露过多内部状态会造成误操作

**解决方案：**

- 通过 Details Customization 隐藏内部缓存字段
- 只保留 TA 需要直接编辑的参数
- 保持参数入口清晰

**结果：**

- 编辑器面板更干净
- 避免误改内部缓存导致异常

---

### 3.6 PCG 输出的中间量选择

**问题：**

- 圆数据在 PCG 中曾使用 `PolyLine / Spline` 作为中间量
- 这会让圆看起来像“分段近似”

**解决方案：**

- 先保证源码版和插件版都能稳定输出可消费的数据
- 在文档中明确当前实现是“分段近似”还是“解析数据”
- 不在不稳定阶段夸大实现效果

**结果：**

- 便于按阶段推进
- 不会因为目标过高导致实现反复回退

---

### 3.7 源码版与插件版共存冲突

**问题：**

- 同名 `CircleComponent`、同名 `Get Circle Data` 容易在同一工程中冲突
- UHT / 编译目标会混淆加载路径

**解决方案：**

- 源码版和插件版分开验证
- 在不同阶段只启用一种实现
- 需要跨 UE 版本验证时优先使用插件版

**结果：**

- 降低命名冲突风险
- 便于在其他 UE 工程中直接验证插件

---

### 3.8 构建过程中的 UBA / Live Coding 问题

**问题：**

- 使用 Visual Studio 调试 UE 时，容易遇到 Live Coding、UBA、旧 DLL 占用导致的编译或运行异常
- 例如内存占用过高、编译任务被 UBA 杀掉、编辑器看不到新变化

**解决方案：**

- 编译前关闭旧的 UE 进程
- 必要时关闭 Live Coding 和 UBA
- 确保编译目标是正确的 `UnrealEditor`，并且编辑器重新启动后再验证

**结果：**

- 编译更稳定
- 新 DLL 能正确被加载

---

## 4. 测试与验证

本项目做过的验证主要有三类：

### 4.1 自动化功能测试

验证内容包括：

- 源码版和插件版 `Get Circle Data` 输出一致性
- `Scale` 语义是否影响结果
- `CircleComponent` 的基本有效性

### 4.2 自动化性能测试

验证内容包括：

- 核心路径耗时
- 插件路径耗时
- helper 路径耗时

### 4.3 构建验证

验证内容包括：

- `UnrealEditor Win64 Development`
- `NoHotReloadFromIDE`
- `NoUBA`

---

## 5. 当前项目结论

### 5.1 已解决的问题

- 能区分源码版和插件版
- `Get Circle Data` 已有明确实现路径
- Details 面板可控
- 基础构建能通过
- 自动化测试和性能基线已经建立

### 5.2 仍然需要保持的原则

- 不要把 UI 语义和内部计算语义混在一起
- 不要同时维护过多相互冲突的实现分支
- 任何大改动都要先保证基础交互稳定，再谈增强功能

### 5.3 最终建议

- 如果目标是项目展示和跨工程使用，优先使用插件版
- 如果目标是验证 UE 源码和底层行为，保留源码版作为参考
- 对外表述时保持克制，不夸大当前实现效果

