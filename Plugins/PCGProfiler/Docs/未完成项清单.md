# UE5.7 PCG Profiler 未完成项清单

更新时间：2026-04-09
仓库：`F:\UnrealEngine-5.7.3-release`
分支：`codex/pcg-profiler`

## A. 高优先级（当前最影响交付）
1. 大规模真实关卡回归基准报告未稳定产出
- 现状：基准脚本已存在并加入超时/重试/进度条，但长跑过程中被中断，未形成最终中文报告文件。
- 目标：稳定产出以下两类文件：
  - `大规模真实关卡_统计稳定性性能回归报告_*.md`
  - `基准结果汇总_*.json`

2. 长时运行残留进程清理机制需纳入流程
- 现状：多次中断后出现 `UnrealEditor-Cmd` 残留进程。
- 目标：基准启动前自动清理残留进程，避免数据污染与资源占用。

## B. 中优先级（增强可信度）
1. 基准结果对比“基线版本”机制
- 现状：已能做单次批量采样统计。
- 目标：支持“本次 vs 基线”自动判定（回归/通过），并写入中文报告。

2. 失败样本日志定位增强
- 现状：报告中会记录失败样本及日志路径。
- 目标：增加失败原因分类（超时/命令失败/无导出文件/JSON解析失败）。

## C. 低优先级（工程化优化）
1. 基准测试参数模板化
- 现状：脚本参数可传，但无预设模板。
- 目标：提供 `quick/full` 两套配置模板（轮次、超时、阈值）。

2. 报告可读性增强
- 现状：Markdown 表格 + JSON 原始结果。
- 目标：增加“Top 波动节点”与“关键路径波动摘要”章节。

## 已有资产（可直接复用）
- 归档文档：`Plugins/PCGProfiler/Docs/已实现功能归档.md`
- 基准脚本：`Plugins/PCGProfiler/Scripts/Run-PCGProfilerBenchmark.ps1`
- 基准目录（历史）：`F:\Unreal Projects\ElectricDreamsEnv\Saved\Profiling\PCG\Benchmark\*`

## 完成判定（针对本清单）
满足以下条件即视为本清单闭环：
1. 成功跑完 `2图 x 2轮` 并生成中文报告（快速验证）。
2. 成功跑完 `2图 x 5轮` 并生成中文报告（正式版）。
3. 报告包含：样本数、均值、P95、CV、稳定性结论、失败样本明细。
